# bintropy
The Binary Entropy Scanner is a lightweight static analysis tool for threat hunters. It calculates the Shannon entropy of executables via a sliding window to detect packed or encrypted payloads. By mapping byte-level randomness into visual plots, analysts can instantly pinpoint hidden malicious code blocks without needing to execute the file.
